# Base packages — installed on every profile (desktop and server).
# Lines starting with # are comments. Empty lines are ignored.
# Trailing inline comments are stripped before installation.

# Shell and dotfiles
zsh
stow
zsh-autosuggestions
zsh-syntax-highlighting
fzf
tmux
vim

# General toolbox
curl
wget
jq
yq
git
gh                # GitHub CLI (repo configured by 10-packages.sh)
btop
tree
ncal
unzip
xxd
ripgrep
fd-find           # binary is 'fdfind' on Debian
bat               # binary is 'batcat' on Debian
less
man-db
manpages
direnv
httpie
rsync
lsof
strace
build-essential
shellcheck

# Network CLI (no GUI)
# Note: 'nc' from netcat-openbsd is preferred over Trixie's default
# netcat-traditional (better IPv6 support, -e flag for pentesting).
# 10-packages.sh pins update-alternatives accordingly.
nmap
ncat
netcat-openbsd
tcpdump
whois
mtr-tiny
traceroute

# Containers — useful on both workstation and server profile.
# Note: docker-compose (Python v1) was removed from Trixie. docker.io provides
# 'docker compose' (subcommand) via the integrated compose plugin. If
# docker-ce (from download.docker.com) is already installed, 10-packages.sh
# detects the conflict and skips docker.io — the docker CLI + group are
# provided by docker-ce anyway.
docker.io

# GPG (base — YubiKey-specific packages live in desktop.txt)
gnupg
