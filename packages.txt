# Base packages for all machines.
# Lines starting with # are comments. Empty lines are ignored.
# Trailing '# vm-skip' marker excludes a package on hosts where is_vm=yes.

# Shell and dotfiles
zsh
stow
zsh-autosuggestions
zsh-syntax-highlighting
fzf
tmux
vim

# General toolbox
curl
wget
jq
yq
git
gh                # GitHub CLI (repo configured by 10-packages.sh)
btop
tree
unzip
ripgrep
fd-find           # binary is 'fdfind' on Debian
bat               # binary is 'batcat' on Debian
less
man-db
manpages
direnv
httpie
rsync
lsof
strace
build-essential
shellcheck

# Clipboard (X11 + Wayland)
xclip
wl-clipboard

# Network tools
# Note: 'nc' from netcat-openbsd is preferred over Trixie's default
# netcat-traditional (better IPv6 support, -e flag for pentesting).
# 10-packages.sh pins update-alternatives accordingly.
nmap
ncat
netcat-openbsd
tcpdump
whois
wireshark
mtr-tiny
traceroute

# Virtualization (skipped on hosts where is_vm=yes)
qemu-system-x86         # vm-skip
qemu-utils              # vm-skip
libvirt-daemon-system   # vm-skip
libvirt-clients         # vm-skip
virt-manager            # vm-skip
virt-viewer             # vm-skip
bridge-utils            # vm-skip

# Containers
# Note: docker-compose (Python v1) was removed from Trixie. docker.io provides
# 'docker compose' (subcommand) via the integrated compose plugin.
docker.io

# Infrastructure tools
# Note: terraform is not in Debian repos. The HashiCorp apt repository is
# configured by 10-packages.sh before this list is processed.
ansible
terraform

# Remote desktop
rdesktop

# Notes / passwords
keepassxc

# Browser
firefox-esr

# Editor
# Note: VS Code is not in Debian repos. The Microsoft apt repository is
# configured by 10-packages.sh before this list is processed.
code

# YubiKey and GPG
gnupg
scdaemon
yubikey-manager
libpam-u2f
pamu2fcfg

# Obsidian is installed separately (AppImage, not in Debian repos).
